---
layout: layouts/base.njk
---

<div class="fungallas">
  {{ comp.breadcrumb({url: url}) | safe }}
</div>

<div id="cabeceira" class="arriba2 abaixo2">
  <h1>{{ title }}</h1>
  <p class="descricion">
    <span>
    {{ description | md(true) | safe }}
    </span>
  </p>
</div>

<div class="listado">
  {% set sections = search.pages("type=glossary lang=" + lang + " section_id=" + id, "title") %}

  {% if sections.length %}
    {% for section in sections %}
    <hr class="separador" />
    <div class="listado_elemento borrar">
      <h3><a href="{{ section.url }}">{{ section.title }}</a></h3>
      <p class="intro">
        {{ section.description | md(true) | safe }}
      </p>
    </div>
    {% endfor %}
  {% else %}
    {% set words = search.pages("type=word lang=" + lang + " section_id=" + id, "title") %}
    {% for word in words %}
    <hr class="separador" />
    <div class="listado_elemento borrar">
      <h3><a href="{{ word.url }}">{{ word.title }}</a></h3>
    </div>
    {% endfor %}
  {% endif %}
</div>
